id: fixture-import-cwd-escape
name: fixture studio
kinds: [image]
requires: { os: [darwin], arch: [arm64] }
runtime: { framework: native-kernel, backends: [metal] }
repo: https://example.com/fixture
import:
  cwd: "../../.."
  run: "go run ./cmd/import"
processes:
  - name: studio
    role: main
    cmd: "./bin/studio --port {port}"
